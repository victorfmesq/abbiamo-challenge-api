version: '3.8'

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: abbiamo-api
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
      - NODE_ENV=production
    restart: unless-stopped
